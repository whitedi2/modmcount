
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("Регистратор") Тогда
		Список.Параметры.УстановитьЗначениеПараметра("ВыбДок", Параметры.Регистратор);
		ЭтаФорма.Заголовок = Строка(Параметры.Регистратор);
	Иначе	
		Список.Параметры.УстановитьЗначениеПараметра("ВыбДок", Неопределено);
	КонецЕсли;
КонецПроцедуры
