
Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
	
	Попытка
		ТекРекв = БюджетныйНаСервере.ВернутьРеквизит(Данные.Ссылка, "Организация");	
	Исключение
		Возврат;
	КонецПопытки;
	
	Если ЗначениеЗаполнено(Данные) И ЗначениеЗаполнено(ТекРекв) Тогда
		СтандартнаяОбработка = Ложь;
		Представление = Строка(Данные.Наименование) + " (" + Строка(ТекРекв) + ")";
	КонецЕсли;
	
КонецПроцедуры
