
&НаКлиенте
Процедура ПриОткрытии(Отказ)

КонецПроцедуры
	
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	БюджетныйНаСервере.ДействияПриСозданииФормыСправочника(ЭтаФорма);
	Если Параметры.Свойство("ЗначениеКопирования") Тогда
		Если ЗначениеЗаполнено(Параметры.ЗначениеКопирования) Тогда 
			Возврат;
		КонецЕсли;
	КонецЕсли;
			СозданиеИзДокумента = Ложь;
			Если Не ЗначениеЗаполнено(Объект.Ссылка) Тогда
				Если Параметры.Свойство("ЗаполнятьОтборДляВНХ") Тогда
					Если Параметры.ЗаполнятьОтборДляВНХ Тогда
						Объект.Владелец = Параметры.Контрагент;
						Объект.ОрганизацияВладельца = Параметры.ОрганизацияКонтрагента;
						Объект.Предприятие = Параметры.Предприятие;
						СозданиеИзДокумента = Истина;
					КонецЕсли;
				КонецЕсли; 
				Если Не СозданиеИзДокумента Тогда
					Если ЭтотОбъект.Параметры.ПараметрыВыбора.Свойство("Владелец") Тогда
						КонтрагентОтбора = ЭтотОбъект.Параметры.ПараметрыВыбора.Владелец;
						Если ЗначениеЗаполнено(КонтрагентОтбора) И
							ТипЗнч(КонтрагентОтбора) = Тип("СправочникСсылка.Контрагенты") Тогда
							Если КонтрагентОтбора.ОрганизацияВГОВНХ И ЗначениеЗаполнено(КонтрагентОтбора.ЭтоОрганизация) Тогда   
								Объект.ОрганизацияВладельца = КонтрагентОтбора.ЭтоОрганизация;
							КонецЕсли;
						КонецЕсли;
					КонецЕсли; 
				КонецЕсли;
			КонецЕсли;

КонецПроцедуры                               

