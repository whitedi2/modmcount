
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("ДатаТек") Тогда
		Список.Параметры.УстановитьЗначениеПараметра("Дата1", Параметры.ДатаТек) 
	Иначе
		Список.Параметры.УстановитьЗначениеПараметра("Дата1", ТекущаяДата()) 
	КонецЕсли;
	Если Параметры.Свойство("ПредприятиеТек") Тогда
		Список.Параметры.УстановитьЗначениеПараметра("ТекущееПредприятие", Параметры.ПредприятиеТек);
	Иначе
		
		ТекПредприятия = БюджетныйНаСервере.ПолучитьПредприятия();
		
		//Для каждого ТекСтрокаОтбора Из Параметры.Отбор Цикл
		//	Если ТекСтрокаОтбора.Ключ = "Предприятие" ИЛИ ТипЗнч(ТекСтрокаОтбора.Значение) = Тип("СправочникСсылка.Предприятия") Тогда
		//		ТекПредприятия = ТекСтрокаОтбора.Значение;
		//		Параметры.Отбор.Очистить();
		//	КонецЕсли;
		//КонецЦикла;
		
		Список.Параметры.УстановитьЗначениеПараметра("ТекущееПредприятие", ТекПредприятия);
	КонецЕсли;
	
	Список.Параметры.УстановитьЗначениеПараметра("ДоступныВсеПредприятия", НЕ ПараметрыСеанса.ТекущийПользователь.ДоступныПредприятияИзСписка);
	
	
КонецПроцедуры
