
&Вместо("КодыGS1Служебный")
Функция УУ_КодыGS1Служебный()
	// Вставить содержимое метода.
	Результат = ПродолжитьВызов();
	Если ТипЗнч(Результат) = Тип("Соответствие") Тогда
		РедактируемыйЭлемент = Результат.Получить("37");
		Если РедактируемыйЭлемент <> Неопределено Тогда 
			Если РедактируемыйЭлемент.ПеременнаяДлина < 10 Тогда
				РедактируемыйЭлемент.ПеременнаяДлина = 10;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	Возврат Результат;
КонецФункции
