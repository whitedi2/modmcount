
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Элементы.ТипОбъекта.РежимВыбораИзСписка = Истина;
	
	ТипыДокументов = Метаданные.ОпределяемыеТипы.сабОбъектыДоступность.Тип.Типы();
	Для каждого ТекДокумент Из ТипыДокументов Цикл
		ДокСсылка = Новый(ТекДокумент);
		Элементы.ТипОбъекта.СписокВыбора.Добавить(ДокСсылка.Метаданные().Имя, ДокСсылка.Метаданные().Синоним);	
	КонецЦикла; 
КонецПроцедуры
