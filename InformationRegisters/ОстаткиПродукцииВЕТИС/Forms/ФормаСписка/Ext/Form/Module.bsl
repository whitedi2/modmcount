
&НаКлиенте
&Вместо("УстановитьОтборПродукция")
Процедура УУ_УстановитьОтборПродукция()
		ПродукцияОтбор = Неопределено;
	
	Если ЗначениеЗаполнено(Продукция) Тогда
		ПродукцияОтбор = Продукция;
	ИначеЕсли ЗначениеЗаполнено(ВидПродукции) Тогда
		ПродукцияОтбор = ВидПродукции;
	ИначеЕсли ЗначениеЗаполнено(ТипПродукции) Тогда
		ПродукцияОтбор = ТипПродукции;
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
		ЭтаФорма.Список,
		"Продукция",
		ПродукцияОтбор,
		ВидСравненияКомпоновкиДанных.ВИерархии,,
		ЗначениеЗаполнено(ПродукцияОтбор));

КонецПроцедуры

&НаСервере
&Вместо("ЗаполнитьРеквизитыПоПродукции")
Процедура УУ_ЗаполнитьРеквизитыПоПродукции(Продукция)
	РеквизитыПродукции = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(
	Продукция,
	"ТипПродукции,Продукция,ВидПродукции");
	
	ЭтаФорма.Продукция    = Продукция;
	ЭтаФорма.ТипПродукции = РеквизитыПродукции.ТипПродукции;
	ЭтаФорма.ВидПродукции = РеквизитыПродукции.ВидПродукции;
	
КонецПроцедуры
