
&НаКлиенте
Процедура СписокПередНачаломИзменения(Элемент, Отказ)
	
	Отказ = Истина;
    Форма = ПолучитьФорму("РегистрСведений.ОсновныеДолжностиПредприятия.Форма.ФормаЗаписи");
	Форма.Запись.Предприятие = Элемент.ТекущиеДанные.Предприятие;
	Форма.Запись.Должность = Элемент.ТекущиеДанные.Должность;
	Форма.Запись.Сотрудник = Элемент.ТекущиеДанные.Сотрудник;
	Форма.Открыть()
	
КонецПроцедуры

&НаКлиенте
Процедура ИзменитьДату(Команда)
	
	Форма = ПолучитьФорму("РегистрСведений.ОсновныеДолжностиПредприятия.Форма.ФормаЗаписи", Новый Структура("Ключ", Элементы.Список.ТекущаяСтрока));
	Форма.Элементы.Предприятие.Доступность = Ложь;
	Форма.Элементы.Должность.Доступность = Ложь;
	Форма.Элементы.Сотрудник.Доступность = Ложь;
	Форма.Открыть()
	
КонецПроцедуры
