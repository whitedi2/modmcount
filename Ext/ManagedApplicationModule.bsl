Перем АдресХранилищаМаршрута Экспорт;
Перем Трей Экспорт;
Перем ТекПолноеНаим Экспорт; //каталог common

&После("ПередНачаломРаботыСистемы")
Процедура УУ_ПередНачаломРаботыСистемы()
	
	// упр д1 24.03.16
	Если сабОбщегоНазначенияБУХ.ПроверитьДоступМодуля() Тогда
		Отказ = Ложь;
		БюджетныйНаКлиенте.ПередНачаломРаботыСистемы(Отказ);
	КонецЕсли;
	// Конец упр

КонецПроцедуры

&После("ПриНачалеРаботыСистемы")
Процедура УУ_ПриНачалеРаботыСистемы()
	
	// упр д1 24.03.16
	Если сабОбщегоНазначенияБУХ.ПроверитьДоступМодуля() Тогда
		БюджетныйНаКлиенте.ПриНачалеРаботыСистемы();
		Если БюджетныйНаСервере.РольАдминаДоступнаСервер() И Не сабОбщегоНазначенияПовтИсп.ПроверитьУстановкуКлюча() Тогда
			ОткрытьФорму("Справочник.сабМониторВнедрения.ФормаСписка");		
		КонецЕсли;
	КонецЕсли;
	// Конец упр

КонецПроцедуры
