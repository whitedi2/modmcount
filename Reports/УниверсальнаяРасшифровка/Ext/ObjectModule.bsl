

Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	сабОбщегоНазначения.сабПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка, СхемаКомпоновкиДанных, КомпоновщикНастроек);
	 Настройки = КомпоновщикНастроек.Настройки;
	 СписокСчетовКассаБанк = Новый СписокЗначений;
	 СписокСчетовКассаБанк.Добавить(ПланыСчетов.Учетный.Счет50());
	 СписокСчетовКассаБанк.Добавить(ПланыСчетов.Учетный.Счет5001());
	 СписокСчетовКассаБанк.Добавить(ПланыСчетов.Учетный.Счет5101());
	 СписокСчетовКассаБанк.Добавить(ПланыСчетов.Учетный.Счет51());
	 СписокСчетовКассаБанк.Добавить(ПланыСчетов.Учетный.Счет5501());
	 СписокСчетовКассаБанк.Добавить(ПланыСчетов.Учетный.НайтиПоКоду("55"));
	 
	 ПараметрСписокСчетов = Настройки.ПараметрыДанных.Элементы.Найти("СписокСчетовКассаБанк");
	 
	 Если Не ПараметрСписокСчетов = Неопределено Тогда
		 ПараметрСписокСчетов.Значение = СписокСчетовКассаБанк;
		 ПараметрСписокСчетов.Использование = Истина;
	 КонецЕсли;
	 
	//Источник = новый ИсточникДоступныхНастроекКомпоновкиДанных(СКД);
	//
	//КомпоновщикНастроек2 = Новый КомпоновщикНастроекКомпоновкиДанных;
	//КомпоновщикНастроек2.Инициализировать(Источник);
	//КомпоновщикНастроек2.ЗагрузитьНастройки(КомпоновщикНастроек.Настройки);
	//КомпоновщикНастроек2.ЗагрузитьПользовательскиеНастройки(КомпоновщикНастроек.ПользовательскиеНастройки);
	//
	//НастройкиДляКомпоновкиМакета = КомпоновщикНастроек2.ПолучитьНастройки();
	//
	//РасшифровкаФакта = СтандартныеОтчетыКлиентСервер.ПолучитьПараметр(НастройкиДляКомпоновкиМакета, "ЭтоРасшифровкаФакта");
	//
	//Если Не РасшифровкаФакта = Неопределено И РасшифровкаФакта.Значение = Истина Тогда
	//	СКД.НаборыДанных.НаборДанных1.Запрос = СтрЗаменить(СКД.НаборыДанных.НаборДанных1.Запрос, "РегистрБухгалтерии.Бюджетный", "РегистрБухгалтерии.Учетный");
	//	СКД.НаборыДанных.НаборДанных1.Запрос = СтрЗаменить(СКД.НаборыДанных.НаборДанных1.Запрос, " Предприятия ", " ЦФО ");
	//	СКД.НаборыДанных.НаборДанных1.Запрос = СтрЗаменить(СКД.НаборыДанных.НаборДанных1.Запрос, " Подразделение ", " ПодразделениеЦФО ");
	//	СКД.НаборыДанных.НаборДанных1.Запрос = СтрЗаменить(СКД.НаборыДанных.НаборДанных1.Запрос, "БюджетныйОбороты.Предприятия", "БюджетныйОбороты.ЦФО");
	//КонецЕсли;
	//
	//КомпоновщикМакета = Новый КомпоновщикМакетаКомпоновкиДанных;
	//МакетКомпоновки = КомпоновщикМакета.Выполнить(СКД, НастройкиДляКомпоновкиМакета, ДанныеРасшифровки, Неопределено);

	//ПроцессорКомпоновки = Новый ПроцессорКомпоновкиДанных;
	//ПроцессорКомпоновки.Инициализировать(МакетКомпоновки, , ДанныеРасшифровки);    
	//
	//ПроцессорВывода = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент;
	//ПроцессорВывода.УстановитьДокумент(ДокументРезультат);
	//ПроцессорВывода.Вывести(ПроцессорКомпоновки, Истина);
	//	
	//СтандартнаяОбработка = Ложь;
	
КонецПроцедуры

