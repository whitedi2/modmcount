
&НаКлиенте
Процедура ВвестиИЗакрыть(Команда)
	СтруктураПараметров = Новый Структура("Фамилия, Имя, Отчество, ДатаРождения, Пол", Фамилия, Имя, Отчество, ДатаРождения, Пол);
	Закрыть(СтруктураПараметров);
	//Оповестить("ВводФИООсуществлен", СтруктураПараметров);
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Фамилия = Параметры.Фамилия;
	Имя = Параметры.Имя;
	Отчество = Параметры.Отчество;
КонецПроцедуры
