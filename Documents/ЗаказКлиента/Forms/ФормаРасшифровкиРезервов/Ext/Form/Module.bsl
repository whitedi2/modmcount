
&НаКлиенте
Процедура ВремТЧЗаказыВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	
	Документ = Элементы.ВремТЧЗаказы.ДанныеСтроки(Элементы.ВремТЧЗаказы.ТекущаяСтрока).Заказ;
	Если ТипЗнч(Документ) = Тип("ДокументСсылка.ЗаказКлиента") Тогда
		ОткрытьФорму("Документ.ЗаказКлиента.ФормаОбъекта", Новый Структура("Ключ, Номенклатура", Документ, Элементы.ВремТЧЗаказы.ДанныеСтроки(Элементы.ВремТЧЗаказы.ТекущаяСтрока).Номенклатура));
	Иначе	
		ПоказатьЗначение(, Элементы.ВремТЧЗаказы.ДанныеСтроки(Элементы.ВремТЧЗаказы.ТекущаяСтрока).Заказ);
	КонецЕсли;
	
КонецПроцедуры
