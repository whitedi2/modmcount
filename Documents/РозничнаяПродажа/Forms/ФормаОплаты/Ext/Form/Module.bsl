
&НаКлиенте
Процедура УУ_ПриОткрытииПосле(Отказ)
	
	Если ВладелецФормы.ИмяФормы = "Документ.РозничнаяПродажа.Форма.ФормаДокументаОбщая" Тогда	
		НастройкаФорма2 = ПолучитьНастройкуФорма2();
		Если НастройкаФорма2 Тогда
			Если ВладелецФормы.Объект.Свойство("ОплатаФорма2") Тогда
				Если ВладелецФормы.Объект.ОплатаФорма2 Тогда 
					ВидОплаты = "Наличные";
					ВидОплатыПриИзменении(Неопределено);
					Элементы.ВидОплаты.Доступность = Ложь;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьНастройкуФорма2()
	
	Возврат Справочники.сабМониторВнедрения.ПолучитьЗначениеНастройки("КонтрольНоменклатурыДляФормы2ВЗаказеКлиента");
	
КонецФункции
